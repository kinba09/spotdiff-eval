# Copy this file to .env and replace the placeholder with your own token.
# Never commit .env or paste the token into source control.
SPOTDIFF_API_TOKEN=replace-with-your-token

# Needed only if your Python installation does not trust the ASU certificate.
SSL_CERT_FILE=/etc/ssl/cert.pem
